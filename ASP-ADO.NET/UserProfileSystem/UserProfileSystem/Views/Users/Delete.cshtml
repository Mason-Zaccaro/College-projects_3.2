@model UserProfileSystem.Models.User

<h2>Удалить пользователя</h2>

<h3>Вы уверены, что хотите удалить этого пользователя?</h3>
<div>
    <dl>
        <dt>ID:</dt>
        <dd>@Model.Id</dd>

        <dt>Имя пользователя:</dt>
        <dd>@Model.Username</dd>

        <dt>Email:</dt>
        <dd>@Model.Email</dd>

        <dt>Дата создания:</dt>
        <dd>@Model.CreatedAt.ToString("dd.MM.yyyy HH:mm")</dd>

        @if (Model.UserProfile != null)
        {
            <dt>Профиль:</dt>
            <dd>Есть (будет также удалён)</dd>
        }
    </dl>

    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Удалить" style="background-color: #dc3545; color: white; border: none; padding: 8px 16px;" /> |
        <a href="@Url.Action("Index")">Назад к списку</a>
    </form>
</div>